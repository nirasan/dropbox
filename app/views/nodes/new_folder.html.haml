%h1 フォルダの追加
= simple_form_for(@node) do |f|
  = f.error_notification
  .form-inputs
    = f.input :is_folder, as: :hidden
    = f.input :parent_node_id, as: :hidden
    = f.input :name
  .form-actions
    = f.button :submit

= link_to '戻る', node_path(@parent_node)